namespace SpeedEnforcement.Contracts.Api;

/// <summary>Route constants shared by the processor (server) and the desktop client.</summary>
public static class TrafficApiRoutes
{
    public const string TrafficSnapshot = "/api/traffic-snapshot";
}
