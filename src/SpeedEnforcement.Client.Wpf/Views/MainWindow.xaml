<Window x:Class="SpeedEnforcement.Client.Wpf.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:SpeedEnforcement.Client.Core.ViewModels;assembly=SpeedEnforcement.Client.Core"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        Title="Highway Speed Enforcement – Live Dashboard"
        Width="1000" Height="600" MinWidth="700" MinHeight="400">
    <DockPanel>
        <!-- Status bar: connection state + snapshot time -->
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBarItem>
                <TextBlock Text="{Binding Status}" />
            </StatusBarItem>
            <Separator />
            <StatusBarItem>
                <TextBlock Text="{Binding LastUpdatedUtc, StringFormat='Snapshot: {0:HH:mm:ss} UTC', TargetNullValue='Snapshot: –'}" />
            </StatusBarItem>
        </StatusBar>

        <Grid Margin="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Global panel -->
            <GroupBox Grid.Column="0" Header="Global Top 10 – highest average speed (≥ 3 cameras)">
                <DataGrid ItemsSource="{Binding GlobalLeaderboard}" AutoGenerateColumns="False" IsReadOnly="True"
                          HeadersVisibility="Column" CanUserSortColumns="False">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="#" Binding="{Binding Rank}" Width="40" />
                        <DataGridTextColumn Header="Plate" Binding="{Binding NumberPlate}" Width="*" />
                        <DataGridTextColumn Header="Avg km/h" Binding="{Binding AverageSpeed, StringFormat={}{0:F1}}" Width="90" />
                        <DataGridTextColumn Header="Cameras" Binding="{Binding CamerasPassed}" Width="70" />
                    </DataGrid.Columns>
                </DataGrid>
            </GroupBox>

            <!-- Per-camera panel -->
            <GroupBox Grid.Column="2" Header="Per-camera Top 10 – instantaneous speed">
                <DockPanel>
                    <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,6">
                        <TextBlock Text="Camera:" VerticalAlignment="Center" Margin="0,0,6,0" />
                        <ComboBox ItemsSource="{Binding CameraIds}" SelectedItem="{Binding SelectedCameraId}" Width="80" />
                    </StackPanel>
                    <DataGrid ItemsSource="{Binding SelectedCameraLeaderboard}" AutoGenerateColumns="False" IsReadOnly="True"
                              HeadersVisibility="Column" CanUserSortColumns="False">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" Binding="{Binding Rank}" Width="40" />
                            <DataGridTextColumn Header="Plate" Binding="{Binding NumberPlate}" Width="*" />
                            <DataGridTextColumn Header="km/h" Binding="{Binding Speed, StringFormat={}{0:F1}}" Width="90" />
                        </DataGrid.Columns>
                    </DataGrid>
                </DockPanel>
            </GroupBox>
        </Grid>
    </DockPanel>
</Window>
